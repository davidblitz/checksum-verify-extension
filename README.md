# checksum-verify-extension

## How to load Extension temporarily

1. Type `about:debugging` in your Firefox Address Bar
2. Click on "Load Temporary Add-On"
3. Open the manifest.json for this project
